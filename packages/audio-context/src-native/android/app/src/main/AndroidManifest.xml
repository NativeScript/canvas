<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AudioContextDemo">
        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>
        <activity
            android:name="org.nativescript.audiocontext.examples.PannerDemoActivity"
            android:exported="true"
            android:label="Panner Demo" />
        <activity
            android:name="org.nativescript.audiocontext.examples.VisualizerActivity"
            android:exported="true"
            android:label="Visualizer" />
        <activity
            android:name="org.nativescript.audiocontext.examples.ExampleChooserActivity"
            android:exported="true"
            android:label="Examples" >
					<intent-filter>
						<action android:name="android.intent.action.MAIN" />

						<category android:name="android.intent.category.LAUNCHER" />
					</intent-filter>
				</activity>
    </application>

</manifest>
